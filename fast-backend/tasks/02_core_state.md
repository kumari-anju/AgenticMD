# Task: State Definition
Feature: `core`

## Description
Define the LangGraph state schema for "Interaction Details" form data.

## Subtasks
- [ ] Create a Pydantic model for the `InteractionState`.
- [ ] Include fields: `hcp_name`, `date`, `sentiment`, and `shared_materials`.
- [ ] Add a `messages` history for conversational context.
- [ ] Update `task_status_update.md` with task details and status.

